#include <iostream>

int main()
{
    /*
    int sum = 0, val = 1;
    while (val <= 10) {
        sum += val;
        ++val;
        std::cout << sum, val;
    }
    std::cout << "1+到10和为" << sum ;

    //for 
    for (int i = 0; i <= 10; i++) {
        std::cout << "i:" << i<<std::endl;
    }
    */
/*
    int sum = 0, val = 0;
    while (std::cin >> val) 
        sum += val;
    
        std::cout << "sum is:" << sum <<std:: endl;
*/
    /*
    int sum = 0, val = 0;
    while (std::cin >> val) {
        sum += val;
    }
    std::cout << "sum is:" << sum << std::endl;
    */
  




    return 0;
}
